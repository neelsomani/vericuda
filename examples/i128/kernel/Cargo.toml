[package]
name = "cuq_example_i128"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["rlib"]
path = "src/lib.rs"

[dependencies]
cuda_std = { version = "0.2", default-features = false }
